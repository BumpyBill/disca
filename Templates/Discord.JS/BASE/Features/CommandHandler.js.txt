const { CommandHandler } = require("disca");

const commands = new CommandHandler(client, "commands");

commands.RegisterCommand({
  name: "ping",
  aliases: ["test"],
  permissions: {
    bot: ["SEND_MESSAGES"],
    user: [],
  },
  execute(message, client, args) {
    message.channel.send("Pong!");
  },
});
